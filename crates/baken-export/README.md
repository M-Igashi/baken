# baken-export

Device export for Bake'n Deck: writes `PIONEER/rekordbox/export.pdb`, the `PIONEER/USBANLZ` tree and `Contents/` on a USB stick straight from a rekordbox `collection.xml`. See the repository README for `baken expressport`.
